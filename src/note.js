export default class note {
  constructor(title, content) {
    this.title = title;
    this.content = content;
  }
  editNote(title, content) {
    this.title = title;
    this.content = content;
  }
}
